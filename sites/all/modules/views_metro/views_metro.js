(function ($) {
  Drupal.viewsMetro = Drupal.viewsMetro || {};
  Drupal.behaviors.viewsMetro = {
    attach: function (context) {  
      /*
      $('.views-metro-container').isotope({
        itemSelector : '.views-metro-item',
        masonry: {
          columnWidth: 350
        },
        cellsByRow: {
          columnWidth: 320,
          rowHeight: 220
        }
      });
      */
    }
  }
})(jQuery)

